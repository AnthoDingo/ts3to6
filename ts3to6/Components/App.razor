<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- ── Primary SEO ─────────────────────────────────────────────────── -->
    <title>TS3 to TS6 Database Converter — Migrate ts3server.sqlitedb to TeamSpeak 6</title>
    <meta name="description"
          content="Free online tool to convert your TeamSpeak 3 database (ts3server.sqlitedb) to TeamSpeak 6 format (tsserver.sqlitedb). Migrates clients, channels, bans, server groups and properties. Download via browser, curl or wget." />
    <meta name="keywords"
          content="teamspeak 3 to teamspeak 6, ts3 ts6 migration, ts3server.sqlitedb converter, tsserver.sqlitedb, teamspeak database migration, ts3 to ts6 converter, teamspeak 6 migration tool, sqlite teamspeak converter" />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="TS3→TS6 Converter" />

    <!-- ── Open Graph (Discord, Slack, Facebook…) ──────────────────────── -->
    <meta property="og:type"        content="website" />
    <meta property="og:title"       content="TS3 to TS6 Database Converter" />
    <meta property="og:description" content="Upload your ts3server.sqlitedb and get a ready-to-use tsserver.sqlitedb for TeamSpeak 6. Clients, channels, bans, groups — all migrated in seconds." />
    <meta property="og:locale"      content="en_US" />

    <!-- ── Twitter / X card ─────────────────────────────────────────────── -->
    <meta name="twitter:card"        content="summary" />
    <meta name="twitter:title"       content="TS3 to TS6 Database Converter" />
    <meta name="twitter:description" content="Free tool to migrate your TeamSpeak 3 database to TeamSpeak 6 format. Download via browser, curl or wget." />

    <base href="/" />
    <ResourcePreloader />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Geist+Mono:wght@300;400;500;600&family=Geist:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    @* <link rel="stylesheet" href="@Assets["app.css"]" /> *@
    <link rel="stylesheet" href="app.css" />
    <ImportMap />
    <HeadOutlet />
</head>

<body>
    <Routes />
    @* <ReconnectModal /> *@
    @* <script src="@Assets["_framework/blazor.web.js"]"></script> *@
    <script src="_framework/blazor.web.js"></script>
</body>

</html>
